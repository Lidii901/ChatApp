name: Temporary Final Acceptance Fixes

on:
  push:
    branches: [ui-mechat-rework]
    paths:
      - 'tests/fix_chat_override_basis.py'
      - '.github/workflows/temp-fix-chat-override.yml'

permissions:
  contents: write

jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ui-mechat-rework

      - name: Apply final targeted fixes
        run: python3 tests/fix_chat_override_basis.py

      - name: Commit final app fixes
        shell: bash
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add src/App.tsx src/components/ChatMenuDrawer.tsx src/components/CharacterEditorModal.tsx server.ts
          if git diff --cached --quiet; then
            echo 'No changes to commit.'
            exit 0
          fi
          git commit -m 'Fix remaining acceptance issues'
          git push origin HEAD:ui-mechat-rework
